# RAG Chatbot Architecture & Flow

This document explains the complete architecture, data flow, and component interactions of the RAG-powered Health Assistant chatbot.

---

## 🏗️ Overall Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Browser                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Next.js Frontend Application                │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │  │
│  │  │  Dashboard   │  │ Chat UI      │  │  Other Pages │  │  │
│  │  │   Page       │  │ Component    │  │              │  │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────────────┘
                     │ HTTP Requests
                     │
┌────────────────────▼────────────────────────────────────────────┐
│              Next.js API Routes (Backend)                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  /api/chat (route.ts)                                    │  │
│  │  - Receives user messages                                │  │
│  │  - Orchestrates RAG flow                                 │  │
│  │  - Returns AI responses                                  │  │
│  └────────────┬───────────────────────┬────────────────────┘  │
└───────────────┼───────────────────────┼────────────────────────┘
                │                       │
    ┌───────────▼──────────┐  ┌────────▼────────┐
    │  FAISS Service       │  │  Genkit/Gemini  │
    │  (Python Service)    │  │  (AI Flow)      │
    │  http://localhost:   │  │                 │
    │       8000           │  │                 │
    └───────────┬──────────┘  └────────┬────────┘
                │                       │
    ┌───────────▼──────────┐           │
    │  Local File Storage  │           │
    │  - FAISS Indices     │           │
    │  - Metadata JSON     │           │
    └──────────────────────┘           │
                                       │
                            ┌──────────▼────────┐
                            │  Google Cloud     │
                            │  - Embedding API  │
                            │  - Gemini API     │
                            └───────────────────┘
```

---

## 🔄 Complete Data Flow Diagram

### 1. Initial Setup Flow (One-Time)

```
┌─────────────────────────────────────────────────────────────┐
│                    INITIALIZATION FLOW                        │
└─────────────────────────────────────────────────────────────┘

Step 1: Load Data
    ┌────────────────┐
    │  CSV Dataset   │────┐
    │  (Diseases)    │    │
    └────────────────┘    │
                          │
                          ├──► ┌──────────────────────────┐
                          │    │  Data Loader             │
    ┌────────────────┐    │    │  (data_loader.py)        │
    │  Firestore     │────┘    │  - Parses CSV            │
    │  (Records)     │         │  - Loads Firestore       │
    └────────────────┘         │  - Creates chunks        │
                               └──────────┬───────────────┘
                                          │
                                          ▼
Step 2: Generate Embeddings
                               ┌──────────────────────────┐
                               │  Embedding Service       │
                               │  (embeddings.py)         │
                               │  - Calls Google API      │
                               │  - Converts text→vectors │
                               │  - Batch processing      │
                               └──────────┬───────────────┘
                                          │
                                          ▼
Step 3: Create FAISS Index
                               ┌──────────────────────────┐
                               │  FAISS Manager           │
                               │  (faiss_manager.py)      │
                               │  - Creates index         │
                               │  - Adds vectors          │
                               │  - Saves metadata        │
                               └──────────┬───────────────┘
                                          │
                                          ▼
Step 4: Save to Disk
                    ┌─────────────────────────────────┐
                    │  Local File System              │
                    │  - diseases_index.faiss         │
                    │  - patient_records_index.faiss  │
                    │  - metadata/*.json              │
                    └─────────────────────────────────┘
```

---

### 2. Query/Response Flow (Real-Time)

```
┌─────────────────────────────────────────────────────────────┐
│                    QUERY FLOW (Per Message)                  │
└─────────────────────────────────────────────────────────────┘

User Types Question
        │
        ▼
┌──────────────────────────────────────┐
│  HealthChatbot Component             │
│  (Frontend - React)                  │
│  - Captures user input               │
│  - Shows loading state               │
│  - Displays messages                 │
└──────────────┬───────────────────────┘
               │
               │ POST /api/chat
               │ { message, userId, history }
               ▼
┌──────────────────────────────────────┐
│  Next.js API Route                   │
│  (/api/chat/route.ts)                │
│  - Receives message                  │
│  - Validates input                   │
└──────────────┬───────────────────────┘
               │
               ├─────────────────────────────────────┐
               │                                     │
               ▼                                     ▼
┌─────────────────────────────┐    ┌──────────────────────────────┐
│  FAISS Search               │    │  Context Assembly            │
│  (Step 1: Retrieval)        │    │  (Step 2: Preparation)       │
│                             │    │                              │
│  POST /search               │    │  - Formats diseases context  │
│  {                          │    │  - Formats records context   │
│    query: "fever symptoms", │    │  - Prepares for LLM          │
│    top_k: 5,                │    │                              │
│    user_id: "..."           │    │                              │
│  }                          │    │                              │
│                             │    │                              │
│  ┌──────────────────────┐   │    │                              │
│  │  FAISS Service       │   │    │                              │
│  │  (main.py)           │   │    │                              │
│  │                      │   │    │                              │
│  │  1. Embed query      │   │    │                              │
│  │  2. Search indices   │   │    │                              │
│  │  3. Return results   │   │    │                              │
│  └──────┬───────────────┘   │    │                              │
│         │                    │    │                              │
│         ▼                    │    │                              │
│  ┌──────────────────────┐   │    │                              │
│  │  FAISS Index         │   │    │                              │
│  │  - diseases_index    │   │    │                              │
│  │  - records_index     │   │    │                              │
│  └──────────────────────┘   │    │                              │
│                             │    │                              │
│  Returns:                   │    │                              │
│  {                          │    │                              │
│    diseases_results: [...], │    │                              │
│    records_results: [...]   │    │                              │
│  }                          │    │                              │
└──────────────┬──────────────┘    └──────────────┬───────────────┘
               │                                   │
               │                                   │
               └───────────────┬───────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│  Genkit RAG Flow                                             │
│  (chat-rag.ts)                                               │
│                                                               │
│  1. Receives:                                                 │
│     - User message                                            │
│     - Retrieved context (diseases + records)                  │
│     - Conversation history                                    │
│                                                               │
│  2. Creates prompt with context                               │
│                                                               │
│  3. Calls Gemini API                                          │
│     ┌─────────────────────┐                                  │
│     │  Google Gemini      │                                  │
│     │  (Gemini 2.0 Flash) │                                  │
│     │  - Generates answer  │                                  │
│     │  - Uses RAG context  │                                  │
│     └─────────────────────┘                                  │
│                                                               │
│  4. Returns response + sources                                │
└──────────────┬────────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  API Route Returns Response                                   │
│  {                                                            │
│    response: "Based on...",                                   │
│    sources: ["Disease Database: Fever"],                      │
│    timestamp: "2024-01-15T10:30:00Z"                          │
│  }                                                            │
└──────────────┬────────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  HealthChatbot Component                                      │
│  - Displays response                                          │
│  - Shows sources                                              │
│  - Updates conversation history                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Component Interaction Diagram

```
┌──────────────────────────────────────────────────────────────┐
│                      COMPONENT INTERACTIONS                    │
└──────────────────────────────────────────────────────────────┘

┌─────────────┐
│   User      │
└──────┬──────┘
       │ Types message
       ▼
┌──────────────────────────────────────────────────────────────┐
│  Frontend Layer (Next.js)                                     │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────────┐                                        │
│  │ HealthChatbot    │                                        │
│  │ Component        │                                        │
│  │                  │                                        │
│  │ State:           │                                        │
│  │ - messages[]     │                                        │
│  │ - input          │                                        │
│  │ - loading        │                                        │
│  └────────┬─────────┘                                        │
│           │ fetch('/api/chat', ...)                          │
│           ▼                                                   │
│  ┌──────────────────┐                                        │
│  │ /api/chat        │                                        │
│  │ route.ts         │                                        │
│  │                  │                                        │
│  │ Functions:       │                                        │
│  │ - POST()         │                                        │
│  │ - GET()          │                                        │
│  └────┬───────┬─────┘                                        │
│       │       │                                              │
│       │       └──────────────┐                               │
│       │                      │                               │
│       │                      ▼                               │
│       │          ┌──────────────────────┐                    │
│       │          │  FAISS Service       │                    │
│       │          │  (Python/FastAPI)    │                    │
│       │          │                      │                    │
│       │          │  Endpoints:          │                    │
│       │          │  - POST /search      │                    │
│       │          │  - GET /health       │                    │
│       │          │                      │                    │
│       │          │  Components:         │                    │
│       │          │  - embeddings.py     │                    │
│       │          │  - faiss_manager.py  │                    │
│       │          │  - data_loader.py    │                    │
│       │          └──────────┬───────────┘                    │
│       │                     │                                 │
│       │                     ▼                                 │
│       │          ┌──────────────────────┐                    │
│       │          │  Local Storage       │                    │
│       │          │  - *.faiss files     │                    │
│       │          │  - metadata/*.json   │                    │
│       │          └──────────────────────┘                    │
│       │                                                      │
│       ▼                                                      │
│  ┌──────────────────┐                                        │
│  │ chatWithRAG()    │                                        │
│  │ (chat-rag.ts)    │                                        │
│  │                  │                                        │
│  │ Flow:            │                                        │
│  │ 1. Format context│                                        │
│  │ 2. Create prompt │                                        │
│  │ 3. Call Gemini   │                                        │
│  │ 4. Return answer │                                        │
│  └────┬─────────────┘                                        │
│       │                                                      │
│       ▼                                                      │
│  ┌──────────────────┐                                        │
│  │  Genkit/Gemini   │                                        │
│  │  (AI Service)    │                                        │
│  │                  │                                        │
│  │  - Gemini 2.0    │                                        │
│  │  - Embedding API │                                        │
│  └──────────────────┘                                        │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔍 Detailed Step-by-Step Flow

### Step 1: User Input
```
User → Types message in chat UI → HealthChatbot Component
```

### Step 2: API Request
```
HealthChatbot → POST /api/chat → {
  message: "What are fever symptoms?",
  userId: "user123",
  conversationHistory: [...]
}
```

### Step 3: Retrieve Context (RAG - Retrieval)
```
API Route → POST http://localhost:8000/search → {
  query: "What are fever symptoms?",
  top_k: 5,
  search_type: "both",
  user_id: "user123"
}

FAISS Service:
  1. Embed query text → [0.123, -0.456, ...] (768-dim vector)
  2. Search diseases_index → Find similar vectors
  3. Search patient_records_index → Find user's relevant records
  4. Return top 5 results from each
```

### Step 4: Format Context
```
API Route → Formats FAISS results:
  - Diseases: "Disease 1: Fever\nSymptoms: ...\nTreatments: ..."
  - Patient Records: "Record 1: ...\nContent: ..."
```

### Step 5: Generate Response (RAG - Generation)
```
API Route → chatWithRAG() → {
  userMessage: "What are fever symptoms?",
  diseasesContext: "Disease 1: Fever\n...",
  patientRecordsContext: "...",
  conversationHistory: "..."
}

Genkit Flow:
  1. Creates prompt with context
  2. Sends to Gemini API
  3. Gemini generates contextual answer
  4. Returns response + sources
```

### Step 6: Display Response
```
API Route → Returns {
  response: "Based on our knowledge base...",
  sources: ["Disease Database: Fever"],
  timestamp: "..."
}

HealthChatbot → Displays message → User sees answer
```

---

## 🗄️ Data Storage Structure

```
src/ai/rag/
├── indices/
│   ├── diseases_index.faiss          # Vector index (binary)
│   ├── patient_records_index.faiss   # Vector index (binary)
│   └── metadata/
│       ├── diseases_metadata.json    # ID → Disease info mapping
│       └── patient_records_metadata.json  # ID → Record mapping
│
└── faiss-service/
    ├── main.py                       # API server
    ├── embeddings.py                 # Embedding generation
    ├── faiss_manager.py              # Index operations
    └── data_loader.py                # Data loading

Metadata Structure:
{
  "0": {
    "code": "1",
    "name": "Panic disorder",
    "symptoms": "...",
    "treatments": "...",
    "chunk_type": "disease"
  },
  "1": { ... }
}
```

---

## 🔐 Security & Privacy Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY CONSIDERATIONS                    │
└─────────────────────────────────────────────────────────────┘

User Authentication:
  User → Firebase Auth → Verified User ID → Passed to API

Data Isolation:
  - Patient records filtered by userId in FAISS search
  - Only user's own records returned
  - Encrypted data in Firestore (already implemented)

API Security:
  - FAISS service runs locally (no external exposure)
  - Next.js API routes handle authentication
  - Environment variables for sensitive keys
```

---

## ⚡ Performance Flow

```
Query Processing Time Breakdown:

1. User Input → API Route:        ~1ms
2. FAISS Search:                   ~50-200ms
   - Query embedding:             ~20-50ms
   - Vector search:               ~10-50ms
   - Metadata lookup:             ~5-10ms
3. Context Formatting:             ~5ms
4. Gemini API Call:                ~500-2000ms
   - Network latency:             ~100-300ms
   - Generation time:             ~400-1700ms
5. Response Processing:            ~5ms

Total: ~600-2200ms per query

Optimization Points:
- FAISS index caching (already in memory)
- Batch embedding requests
- Response streaming (future enhancement)
```

---

## 🔄 Incremental Update Flow

```
New Patient Record Added:
┌─────────────────────────────────────────────────────────────┐

1. User uploads record → Firestore
   │
   ▼
2. Trigger: New record created
   │
   ▼
3. API Route → POST /add-vectors → FAISS Service
   │
   ▼
4. FAISS Service:
   - Embed record content
   - Add to patient_records_index
   - Update metadata
   - Save to disk
   │
   ▼
5. Index updated → Available for next query

└─────────────────────────────────────────────────────────────┘
```

---

## 📈 Scaling Considerations

```
Current Architecture (Local):
┌─────────────────────────────────────────────────────────────┐
│  Single Server Setup                                          │
│  - FAISS service on localhost                                 │
│  - Indices in local filesystem                                │
│  - Suitable for: 1-100 concurrent users                       │
└─────────────────────────────────────────────────────────────┘

Future Scaling Options:
┌─────────────────────────────────────────────────────────────┐
│  Distributed Setup                                            │
│  - FAISS service on dedicated server                          │
│  - Shared storage (NFS/EBS) for indices                       │
│  - Load balancer for multiple instances                       │
│  - Suitable for: 100+ concurrent users                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Key Components Summary

| Component | Purpose | Technology |
|-----------|---------|-----------|
| **HealthChatbot** | UI for chat interface | React/Next.js |
| **/api/chat** | Orchestrates RAG flow | Next.js API Route |
| **FAISS Service** | Vector search engine | Python/FastAPI |
| **Genkit Flow** | AI response generation | Genkit/Gemini |
| **FAISS Indices** | Vector storage | FAISS (local) |
| **Embedding Service** | Text → Vector conversion | Google Embedding API |
| **Data Loader** | Load CSV/Firestore data | Python |

---

## 🔗 External Dependencies

```
┌─────────────────────────────────────────────────────────────┐
│  External Services                                            │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  1. Google Embedding API                                      │
│     - Converts text to vectors                                │
│     - Used during:                                            │
│       * Initial index creation                                │
│       * Query embedding                                       │
│       * Incremental updates                                   │
│                                                               │
│  2. Google Gemini API                                         │
│     - Generates AI responses                                  │
│     - Uses RAG context                                        │
│     - Returns formatted answers                               │
│                                                               │
│  3. Firebase/Firestore                                        │
│     - Stores patient records                                  │
│     - User authentication                                     │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

This architecture provides a complete, scalable RAG system for health-related queries!

